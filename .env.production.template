# =================================================================
# ZIENSHIELD - CONFIGURACIÓN DE PRODUCCIÓN
# =================================================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo como .env.production
# 2. Completa todas las variables OBLIGATORIAS
# 3. Revisa las variables opcionales según tu entorno
# 4. NUNCA commits este archivo con credenciales reales
#
# =================================================================

# =================================================================
# CONFIGURACIÓN DE LA APLICACIÓN
# =================================================================
NODE_ENV=production
APP_NAME=ZienShield
APP_VERSION=1.0.0

# =================================================================
# CONFIGURACIÓN DEL SERVIDOR
# =================================================================
# Puerto del backend (disponible en servidor destino)
PORT=3001
HOST=0.0.0.0
BASE_URL=https://tu-dominio.com

# =================================================================
# CONFIGURACIÓN DE BASE DE DATOS - *** OBLIGATORIO ***
# =================================================================
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=zienshield
DB_USER=your-db-username
DB_PASSWORD=your-secure-db-password
DB_SSL=true

# Pool de conexiones
DB_POOL_MAX=15
DB_POOL_MIN=5
DB_POOL_IDLE=10000
DB_POOL_ACQUIRE=30000

# =================================================================
# CONFIGURACIÓN JWT - *** OBLIGATORIO ***
# =================================================================
# GENERAR UN SECRET FUERTE: openssl rand -base64 64
JWT_SECRET=your-super-secure-jwt-secret-key-here-minimum-32-characters
JWT_EXPIRE=24h

# =================================================================
# CONFIGURACIÓN WAZUH - *** OBLIGATORIO ***
# =================================================================
WAZUH_API_URL=https://your-wazuh-server:55000
WAZUH_USERNAME=your-wazuh-username
WAZUH_PASSWORD=your-wazuh-password
WAZUH_SSL_VERIFY=true
WAZUH_TIMEOUT=10000
WAZUH_RETRY_ATTEMPTS=5
WAZUH_RATE_LIMIT=2

# Cache Wazuh
WAZUH_CACHE_ENABLED=true
WAZUH_CACHE_MAX_MEMORY_MB=100
WAZUH_CACHE_MAX_ENTRIES=1000
WAZUH_CACHE_CLEANUP_INTERVAL_MS=300000

# TTL Cache (milisegundos)
WAZUH_CACHE_TTL_HARDWARE=86400000
WAZUH_CACHE_TTL_OS=43200000
WAZUH_CACHE_TTL_PACKAGES=21600000
WAZUH_CACHE_TTL_NETWORK=1800000
WAZUH_CACHE_TTL_AGENTS=120000
WAZUH_CACHE_TTL_STATS=300000

# =================================================================
# CONFIGURACIÓN CORS - *** OBLIGATORIO ***
# =================================================================
# Dominios permitidos (separados por comas)
CORS_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com
CORS_CREDENTIALS=true
CORS_METHODS=GET,POST,PUT,DELETE
CORS_ALLOWED_HEADERS=Content-Type,Authorization

# =================================================================
# CONFIGURACIÓN RATE LIMITING
# =================================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50
RATE_LIMIT_AUTH_MAX=3
RATE_LIMIT_MESSAGE=Rate limit exceeded. Please try again later.

# =================================================================
# CONFIGURACIÓN DE SEGURIDAD
# =================================================================
HELMET_ENABLED=true
CORS_POLICY=cross-origin

# =================================================================
# CONFIGURACIÓN DE LOGS
# =================================================================
LOG_LEVEL=info
LOG_CONSOLE_ENABLED=true
LOG_FILE_ENABLED=true
LOG_FILE_PATH=./logs
LOG_MAX_FILE_SIZE=50MB
LOG_MAX_FILES=10

# =================================================================
# CONFIGURACIÓN DE MÉTRICAS
# =================================================================
METRICS_ENABLED=true
PROMETHEUS_ENABLED=true
METRICS_PATH=/metrics

# =================================================================
# CONFIGURACIÓN DE VULNERABILIDADES
# =================================================================
VULN_UPDATE_ENABLED=true
VULN_UPDATE_INTERVAL_MS=3600000
VULN_BATCH_SIZE=50

# =================================================================
# CONFIGURACIÓN FRONTEND REACT
# =================================================================
REACT_APP_API_URL=https://tu-dominio.com:3001
REACT_APP_VERSION=1.0.0
REACT_APP_ENVIRONMENT=production

# =================================================================
# CONFIGURACIÓN DOCKER ESPECÍFICA
# =================================================================
# Configuración de red Docker
DOCKER_NETWORK_SUBNET=172.20.0.0/16
DOCKER_BACKEND_IP=172.20.0.10
DOCKER_FRONTEND_IP=172.20.0.20
DOCKER_REDIS_IP=172.20.0.30

# =================================================================
# CONFIGURACIÓN OPCIONAL PARA SERVICIOS EXTERNOS
# =================================================================
# Si usas servicio de email
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=true

# Si usas servicio de notificaciones
NOTIFICATION_WEBHOOK_URL=
NOTIFICATION_SLACK_TOKEN=

# =================================================================
# VARIABLES DE ENTORNO PARA DESARROLLO (NO USAR EN PRODUCCIÓN)
# =================================================================
# Estas variables están comentadas porque NO deben usarse en producción
# Descomenta solo para desarrollo local

# DB_HOST=localhost
# DB_PASSWORD=password
# JWT_SECRET=development-key-not-secure
# WAZUH_API_URL=https://development-wazuh:55000
# WAZUH_USERNAME=wazuh
# WAZUH_PASSWORD=wazuh
# WAZUH_SSL_VERIFY=false
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000